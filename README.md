# Working universal example

Install and run it:

```sh
npm install
npm start
```

Open in browser:

* normal (not prerendered): http://localhost:4200
* universal (prerendered): http://localhost:4000
